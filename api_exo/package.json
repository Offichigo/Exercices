{
  "name": "ada-fetch-async",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test-ui": "vitest --ui",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.2",
    "gh-pages": "^6.3.0",
    "vite": "^8.0.1",
    "vitest": "^4.1.2"
  }
}
